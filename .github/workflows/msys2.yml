name: msys2 build
# https://stackoverflow.com/a/75758843

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-2019
    defaults:
      run:
        shell: bash

    steps:
    - uses: actions/checkout@v4

    - name: gcc version
      run: gcc --version
    - name: all files in vm
      run: dir /s c:\

    - name: Build
      run: make -C RunCPM msys2 build
